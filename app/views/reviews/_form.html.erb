<%= simple_form_for [@group,@post] do |f| %>
    <div class="form-group">
    <%= f.input :content,label: "評論內容" ,input_html: { class: "form-control"} %>
    </div>
    <div class="form-actions">
    <%= f.submit "送出", disable_with: "送出...", class: "btn btn-primary"%>
    </div>
<% end %>
